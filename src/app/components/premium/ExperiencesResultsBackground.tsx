export default function ExperiencesResultsBackground() {
  // Intentionally disabled: after reordering the Method and Projects sections,
  // this background created a large duplicate visual block above the projects.
  // Keep the component as a no-op so existing imports remain stable.
  return null;
}
