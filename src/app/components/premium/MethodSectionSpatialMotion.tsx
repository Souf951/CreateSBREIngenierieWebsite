export default function MethodSectionSpatialMotion() {
  // Intentionally kept as a no-op.
  // The native <details>/<summary> accordion in the Method section must remain
  // fully interactive. Any future visual motion should be applied around the
  // accordion rather than transforming its interactive elements.
  return null;
}
